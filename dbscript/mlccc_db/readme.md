- find the php.ini and find below

`;extension=pgsql`
and
`;extension=php_pgsql.dll`

- Uncomment the line by removing the semicolon (;) at the beginning:

`extension=pgsql`
`extension=php_pgsql.dll`

